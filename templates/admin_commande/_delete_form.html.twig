<form method="post" action="{{ path('app_admin_commande_delete', {'id': commande.id}) }}" onsubmit="return confirm('Voulez vous craiment supprimer cette commande?');" class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande.id) }}">
    <button class="btn btn-danger my-2"><i class="align-middle" data-feather="trash"></i></button>
</form>
