{% extends 'base-backend.html.twig' %}

{% block title %}Edit ContactContent{% endblock %}

{% block body %}
    <a href="{{ path('app_admin_contact_content_index') }}" class="btn btn-dark btn-sm mb-3">Retour à la liste</a>
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1>Modifier les informations de contact</h1>
        {{ include('admin_contact_content/_delete_form.html.twig') }}
    </div>
    {{ include('admin_contact_content/_form.html.twig', {'button_label': 'Modifier'}) }}

    

    
{% endblock %}
