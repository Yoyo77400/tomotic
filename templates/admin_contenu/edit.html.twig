{% extends 'base-backend.html.twig' %}

{% block title %}Edit Contenu{% endblock %}

{% block body %}
    <a href="{{ path('app_admin_contenu_index') }}" class="btn btn-dark btn-sm">Retour à la liste</a>
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h1>Modifier ce contenu</h1>
        {{ include('admin_contenu/_delete_form.html.twig') }}
    </div>
    {{ include('admin_contenu/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}
